## Overview

These APIs use OAuth 2.0 for authentication.

To get started, follow the [Authorization steps](docs/oauth/Authorization.md).

### Standard response fields

Below are the standard fields included in every response.

#### TraceId

A `traceId` is a unique identifier for each request and is returned with every response. Use it to pinpoint the exact request when debugging.
